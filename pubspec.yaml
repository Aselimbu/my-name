name: weather
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Adjusted for broader compatibility

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2 # Updated to a more recent version
  cupertino_icons: ^1.0.8
  intl: ^0.19.0 # Updated to the latest version
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0 # Corrected to the latest available version

flutter:
  uses-material-design: true